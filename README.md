# StaticMessagesSystem
A static messages system using react redux node &amp; mongodb

/root yarn server => launch server

/client
yarn start => launch client

Use the users icon to grab a valid user id.
